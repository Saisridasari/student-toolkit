package com.studenttoolkit.backend;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Basic Spring Boot context load test.
 * Verifies that the application context loads successfully with all configurations.
 */
@SpringBootTest
class BackendApplicationTests {

    @Test
    void contextLoads() {
        // If this test passes, the Spring Boot application context loaded successfully
        // This means all beans, configurations, and dependencies are properly wired
    }
}
